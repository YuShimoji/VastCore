以下のタスクチケットを読み、実装を実行してください。

# Task Ticket
`docs/tasks/TASK_033_DualGridHeightMap_ProfileMappingImplementation.md`

# 実行指示
1. チケットの Objective / Scope / DoD / Constraints を厳守する。
2. `docs/02_design/DUALGRID_HEIGHTMAP_PROFILE_MAPPING_SPEC.md` を実装仕様のSSOTとして扱う。
3. 実装対象は Focus Area に限定し、不要なリファクタは禁止。
4. Unity Editor での compile/test は本タスクでは必須にしない（未実施なら報告に明記）。
5. 作業完了後に以下を必ず実施する:
   - `docs/04_reports/REPORT_TASK_033_DualGridHeightMapProfileMappingImplementation.md` を作成
   - チケット Status を `DONE` に更新
   - 主要変更点と未実施検証を簡潔に記載

# 注意
- 既存エラー修正の横展開は禁止（本タスクの実装範囲外）。
- ブロッカーが出た場合は、原因・影響・次アクションをレポートに残して停止する。
