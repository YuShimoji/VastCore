# VastCore Remote Resume Handoff - 2026-06-26

This file is intentionally ASCII-safe so another terminal can read it through
Windows PowerShell without encoding loss.

## Resume Target

| item | value |
| --- | --- |
| repository | `YuShimoji/VastCore` |
| remote | `origin` = `https://github.com/YuShimoji/VastCore.git` |
| branch to resume | `codex/vc-rst-2e-upm-root-cause` |
| local diagnostic worktree | `C:\Users\PLANNER007\VastCore\VastCore-origin-main-parent-20260625` |
| base before this handoff commit | `e90a4af89b1c75bd7812616439d56630d76e07dc` |
| handoff worktree to leave alone | `C:\Users\PLANNER007\VastCore\VastCore` |
| handoff branch | `codex/vc-rst-remote-handoff-20260622` |
| stale worktree to avoid | `C:\Users\PLANNER007\VastCore\VastCore-origin-main-compile` |

Use the diagnostic parent worktree for Unity Package Manager restoration work.
Do not run package diagnostics from the handoff worktree or the stale compile
worktree.

## Current Diagnosis

Unity Package Manager still fails before C# compile with:

```text
The "path" argument must be of type string. Received undefined
Failed to update project manifest: The "path" argument must be of type string. Received undefined
```

The failure is not currently explained by VastCore package contents. It
reproduces in a clean control project with empty dependencies, survives clean
`UPM_CACHE_ROOT`, and survives ACL XML/etag regeneration.

## Latest Outcome

| slice | result | file |
| --- | --- | --- |
| `VC-RST-2g-rerun-upm-acl-regeneration-after-unity-close` | ACL XML/etag were backed up, renamed, regenerated by Unity, then the clean control project still failed. Original ACL XML/etag were restored. | `docs/restart/VC_UPM_ACL_REGENERATION_RERUN_REPORT.md` |
| `VC-RST-2h-upm-editor-install-or-global-state-repair-decision` | Non-destructive decision packet created. Recommended next step is an Agent-owned short ASCII path control project test before user-visible repair/reinstall actions. | `docs/restart/VC_UPM_ENVIRONMENT_REPAIR_DECISION.md` |

## Files Preserving Context

Read these in order when resuming:

| file | role |
| --- | --- |
| `docs/restart/VC_REMOTE_RESUME_HANDOFF_20260626.md` | This current resume note |
| `docs/restart/VC_UPM_ENVIRONMENT_REPAIR_DECISION.md` | Current decision packet and next action |
| `docs/restart/VC_UPM_ACL_REGENERATION_RERUN_REPORT.md` | Completed ACL regeneration rerun and failed control retest |
| `docs/restart/VC_UPM_ACL_REGENERATION_REPORT.md` | Earlier ACL gate safety stop |
| `docs/restart/VC_UPM_ENVIRONMENT_ISOLATION_REPORT.md` | Clean control project and clean UPM cache-root evidence |
| `docs/restart/VC_PACKAGE_MANAGER_ROOT_CAUSE_REPORT.md` | Minimal manifest, settings, and package-root-cause evidence |
| `docs/restart/VC_PACKAGE_MANAGER_RESTORATION_REPORT.md` | Earlier package restoration attempts |
| `docs/restart/VC_ORIGIN_MAIN_PARENT_BOOTSTRAP_REPORT.md` | Parent worktree bootstrap context |

## Local Artifacts

Some local artifacts/logs are git-ignored. The reports above preserve the
important conclusions. If resuming on the same machine, useful local paths are:

| path | purpose |
| --- | --- |
| `artifacts/restart/t2g-rerun-acl-backup-20260626-135833/` | Backup copies of original ACL XML/etag before regeneration |
| `artifacts/restart/t2g-rerun-acl-rename-ledger-20260626-135833.json` | ACL backup/rename hash ledger |
| `artifacts/restart/t2g-rerun-acl-restore-ledger-20260626-135833.json` | Restore ledger after failed ACL regeneration |
| `artifacts/logs/t2g-rerun-control-acl-regeneration-20260626-135833/control-open-after-acl-rename.log` | Control project failure after ACL regeneration |
| `artifacts/restart/t2h-environment-repair-decision-inputs.json` | Sanitized input summary for T+2h decision |

Do not expose raw Unity Hub or Editor access tokens. Current committed reports
avoid token values.

## What Was Not Changed

| area | state |
| --- | --- |
| `Packages/manifest.json` | no final diff |
| `Packages/packages-lock.json` | no final diff |
| `ProjectSettings/` | no final product diff |
| `Assets/` | no final product diff |
| runtime/editor C# | not modified |
| scenes/prefabs/materials/textures/meshes/imported assets | not modified |
| Unity ACL XML/etag | original active files restored after failed regeneration |
| handoff branch worktree | left clean |

## Recommended Next Slice

Run this next:

```text
VC-RST-2i-short-path-control-project-test
```

Recommended scope:

1. Create a disposable control project outside the VastCore repo family in a
   short ASCII path, for example:

   ```text
   C:\Users\PLANNER007\vc-upm-short\ctrl-600036
   ```

2. Use Unity `6000.3.6f1` first.
3. Keep the project empty/default.
4. Run a package-resolution/import check and capture Unity/UPM logs.
5. Do not modify VastCore packages or code.
6. Do not repair/reinstall Unity, sign out/in Unity Hub, delete global caches,
   or rename more machine-level files in that slice.

Interpretation:

| short-path result | next move |
| --- | --- |
| passes package resolution | path/location/update-dependencies context becomes leading; plan a careful VastCore retest |
| fails with same error | escalate to user-visible environment repair decision: Hub/license refresh, installed-version matrix, Editor repair, or clean Windows user/profile control |

## Do Not Start Yet

| area | reason |
| --- | --- |
| C# compile fixes | C# compile has not been reached |
| terrain / DualGrid / mining / CSG / EasyRoads | outside package restoration |
| Simulator split / Trail / player controller | outside package restoration |
| package removals | clean control project fails independently of VastCore package contents |
| Unity repair/reinstall | not selected until short-path control result is known |
| Unity Hub sign out/in | not selected until short-path control result is known |
| global cache deletion | not authorized and not selected |

## Quick Verification Commands

Run from `C:\Users\PLANNER007\VastCore\VastCore-origin-main-parent-20260625`:

```powershell
git status --short --branch --untracked-files=all
git rev-parse HEAD
git diff --name-status -- Packages ProjectSettings Assets
```

Run from `C:\Users\PLANNER007\VastCore\VastCore`:

```powershell
git status --short --branch
```

Expected after this handoff commit:

| check | expected |
| --- | --- |
| parent branch | `codex/vc-rst-2e-upm-root-cause` tracking `origin/codex/vc-rst-2e-upm-root-cause` |
| parent product diffs | none in `Packages`, `ProjectSettings`, or `Assets` |
| handoff branch | clean |
| next action | `VC-RST-2i-short-path-control-project-test` |

