name: CI (Shared Smoke)

on:
  push:
    branches:
      - main
      - develop
      - feature/**
      - chore/**
  pull_request:

jobs:
  smoke:
    # Removed: shared-workflows submodule was deleted
    # uses: YuShimoji/shared-workflows/.github/workflows/ci-smoke.yml@HEAD
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI workflow disabled - shared-workflows removed"
