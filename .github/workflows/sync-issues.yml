name: Sync Issues (Shared)

on:
  push:
    paths:
      - 'docs/ISSUES.md'
  workflow_dispatch:

jobs:
  sync:
    # Removed: shared-workflows submodule was deleted
    # uses: YuShimoji/shared-workflows/.github/workflows/sync-issues.yml@HEAD
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Sync workflow disabled - shared-workflows removed"
